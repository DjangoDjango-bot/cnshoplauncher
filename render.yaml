# File: render.yaml  (free tier–compatible)
services:
  - type: web
    name: cn-shop-launcher
    env: node
    plan: free
    buildCommand: npm ci
    startCommand: node server.js
    healthCheckPath: /health
    autoDeploy: true
